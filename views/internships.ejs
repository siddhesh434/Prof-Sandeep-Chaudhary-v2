
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<style>
  /* Internship Opportunities Section */
  .internship-section {
    display: flex;
    flex-direction: column;
    gap: 30px;
  }

  .internship-section h2 {
    font-size: 2rem;
    margin-bottom: 25px;
    position: relative;
    padding-bottom: 10px;
  }

  .internship-section h2::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 50px;
    height: 3px;
    background-color: var(--primary-color);
  }

  .internship-intro {
    font-size: 1.1rem;
    line-height: 1.6;
    margin-bottom: 10px;
  }

  .internship-details {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 25px;
    margin: 20px 0;
  }

  .internship-card {
    background-color: var(--secondary-bg);
    border-radius: 10px;
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    border: 1px solid var(--border-color);
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  .internship-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  }

  .card-header {
    background-color: var(--primary-color);
    padding: 15px 20px;
    display: flex;
    align-items: center;
    gap: 15px;
  }

  .card-icon {
    font-size: 1.8rem;
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .card-header h3 {
    color: white;
    margin: 0;
    font-size: 1.3rem;
  }

  .card-content {
    padding: 20px;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    gap: 15px;
  }

  .card-content p {
    margin: 0;
    line-height: 1.6;
  }

  .card-content ul,
  .card-content ol {
    padding-left: 20px;
    margin: 10px 0;
  }

  .card-content li {
    margin-bottom: 10px;
    line-height: 1.5;
  }

  .note {
    font-style: italic;
    opacity: 0.8;
    margin-top: 10px;
  }

  .warning {
    padding: 15px;
    background-color: rgba(255, 193, 7, 0.1);
    border-left: 4px solid #ffc107;
    border-radius: 4px;
    margin-top: 15px;
  }

  .process-steps {
    list-style: none;
    padding: 0;
    counter-reset: step-counter;
  }

  .process-steps li {
    display: flex;
    align-items: flex-start;
    gap: 15px;
    padding: 5px 0;
  }

  .step-number {
    background-color: var(--primary-color);
    color: white;
    width: 30px;
    height: 30px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    flex-shrink: 0;
  }

  .step-desc {
    flex-grow: 1;
    padding-top: 5px;
  }

  .info-list {
    list-style-type: none;
    padding-left: 0;
  }

  .info-list li {
    padding-left: 25px;
    position: relative;
    margin-bottom: 12px;
  }

  .info-list li::before {
    content: "•";
    color: var(--primary-color);
    font-weight: bold;
    position: absolute;
    left: 0;
    top: 0;
    font-size: 1.2rem;
  }

  .internship-cta {
    display: flex;
    gap: 20px;
    margin-top: 20px;
    flex-wrap: wrap;
  }

  .apply-button,
  .email-button {
    padding: 12px 25px;
    border-radius: 50px;
    text-decoration: none;
    font-weight: 500;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }

  .apply-button {
    background-color: var(--primary-color);
    color: white;
  }

  .email-button {
    background-color: transparent;
    color: var(--primary-color);
    border: 2px solid var(--primary-color);
  }

  .apply-button:hover,
  .email-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
  }

  .apply-button:hover {
    background-color: var(--primary-color);
    opacity: 0.9;
  }

  .email-button:hover {
    background-color: rgba(var(--primary-color-rgb), 0.1);
  }

  @media (max-width: 768px) {
    .internship-details {
      grid-template-columns: 1fr;
    }

    .internship-cta {
      flex-direction: column;
    }

    .apply-button,
    .email-button {
      width: 100%;
    }
  }
</style>

<!-- Internship Opportunities Section -->
<section class="section internship-section" style="margin-bottom: 0px">
  <h2>Internship Opportunities</h2>

  <div class="internship-intro">
    <p>
      Internship is governed by the norms of the institute. The process
      typically involves selecting an area you wish to work on, writing an email
      to the concerned faculty, and registering with the institute.
    </p>
  </div>

  <div class="internship-details">
    <div class="internship-card">
      <div class="card-header">
        <div class="card-icon">
          <i class="fas fa-calendar-alt"></i>
        </div>
        <h3>Duration</h3>
      </div>
      <div class="card-content">
        <p>The internship period can be of any duration:</p>
        <ul>
          <li>1-2 month internship for short-term experimental projects</li>
          <li>Semester-long internship for more comprehensive work</li>
          <li>UG and PG dissertation work as part of the internship</li>
        </ul>
        <p class="note">
          Please check with your parent institution regarding the duration
          before applying.
        </p>
      </div>
    </div>

    <div class="internship-card">
      <div class="card-header">
        <div class="card-icon">
          <i class="fas fa-clipboard-list"></i>
        </div>
        <h3>Application Process</h3>
      </div>
      <div class="card-content">
        <ol class="process-steps">
          <li>
            <span class="step-number">1</span>
            <span class="step-desc">Select an area you wish to work on</span>
          </li>
          <li>
            <span class="step-number">2</span>
            <span class="step-desc"
              >Write an email to the concerned faculty</span
            >
          </li>
          <li>
            <span class="step-number">3</span>
            <span class="step-desc">Register with the institute</span>
          </li>
          <li>
            <span class="step-number">4</span>
            <span class="step-desc">Complete the internship</span>
          </li>
          <li>
            <span class="step-number">5</span>
            <span class="step-desc">Receive a certificate upon completion</span>
          </li>
        </ol>
      </div>
    </div>

    <div class="internship-card">
      <div class="card-header">
        <div class="card-icon">
          <i class="fas fa-info-circle"></i>
        </div>
        <h3>Important Information</h3>
      </div>
      <div class="card-content">
        <ul class="info-list">
          <li>Internships can be undertaken throughout the year</li>
          <li>A certificate will be issued at the end of the internship</li>
          <li>
            Internships are typically unpaid; paid internships will be announced
            separately
          </li>
          <li>Interested applicants can apply through the institute website</li>
        </ul>
        <p class="warning">
          In case the application page is not working or if there are changes in
          guidelines, please request the latest guidelines through email.
        </p>
      </div>
    </div>
  </div>

  <div class="internship-cta">
    <a
      href="https://www.iiti.ac.in/page/summer-internship-introduction"
      class="apply-button"
      >Apply for Internship</a
    >
    <a href="mailto:schaudhary@iiti.ac.in" class="email-button"
      >Request Information</a
    >
  </div>
</section>


<script>
  document.addEventListener("DOMContentLoaded", function () {
    // Add animated entrance for cards
    const internshipCards = document.querySelectorAll(".internship-card");

    if ("IntersectionObserver" in window) {
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry, index) => {
            if (entry.isIntersecting) {
              // Add staggered animation delay
              setTimeout(() => {
                entry.target.style.opacity = 1;
                entry.target.style.transform = "translateY(0)";
              }, index * 100);
              observer.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.1 }
      );

      internshipCards.forEach((card) => {
        card.style.opacity = 0;
        card.style.transform = "translateY(20px)";
        card.style.transition = "opacity 0.5s ease, transform 0.5s ease";
        observer.observe(card);
      });
    }

    // Ensure compatibility with theme toggle
    const themeToggle = document.querySelector(".theme-switch input");
    if (themeToggle) {
      // Using CSS variables ensures theme changes are applied automatically
      // This is just for any additional theme-specific adjustments if needed
    }
  });
</script>
